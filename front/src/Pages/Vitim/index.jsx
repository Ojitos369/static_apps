import { useVars, useMyEffects } from "./myUse";

export const Vitim = props => {
    // const {  } = useVars();

    useMyEffects();
    return (
        <div>
            Base Component
        </div>
    );
};

